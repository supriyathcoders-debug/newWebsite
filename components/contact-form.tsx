"use client";

// Contact form intentionally removed per client instruction.
// Keep a small stub export so importing modules won't break the build unexpectedly.
export function ContactForm() {
  return null;
}
